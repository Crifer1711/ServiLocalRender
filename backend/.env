# Para desarrollo local (PostgreSQL local)
DATABASE_URL=postgresql://postgres:password@localhost:5432/servilocal2

# Para producción en Render, copia la DATABASE_URL de tu PostgreSQL en Render
# DATABASE_URL=postgresql://user:password@host:port/database

NODE_ENV=development
PORT=5000